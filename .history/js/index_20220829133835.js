
function getMaxDigit(){
    let randomDigit = Math.floor(Math.random());
    for (var i = 0; i < randomDigit.length; i++) {
        let res = range[i];
        console.log(res);
    }
    debugger;
}